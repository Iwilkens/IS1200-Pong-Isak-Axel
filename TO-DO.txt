DISPLAY
Skärmen har 32x 128 pixlar. Skärmen har 512 minnesbytes för alla pixlar (4096 bitar, 32x128).

PORTF = Button 1 längst till höger.

Button 1 = buttons (8).
Button 2 = buttons (4).
Button 3 = buttons (2).
Button 4 = buttons (1).

** ATT TESTA **

Ändra if statement så den använder ">=" och "<=" istället för ">" och "<".
if (((ball_y + ball_shape) >= paddle1_y) && (ball_y - ball_shape) <= (paddle1_y + paddle_height))

---

Testa ändra if satsen som ser till att bollen inte limmas fast i taket av oled skärmen.
Egentligen samma fast utan count variablen.

if(ball_speed_y < 0.3) && (ball_y <= 10))
{
 ball_speed_y = 1;
}

---

Ta bort dessa minus för att det inte ska vara så likt.
int ball_x = 128 / 2 - 5
float paddle2_y = 32 / 2 - 2;
float paddle1_y = 32 / 2 - 2;

Vi skulle också kunna testa att ändra värdet på paddles storlek / bollen med någon pixel?

---

Tycker vi ska invertera logiken för denna. (paddle 1 och paddle 2 collision).
Kommer ej spela någon roll för hur spelet spelas.

 if(paddle1_up == 1)
  {
	if(ball_speed_y < 0)
    {
      ball_speed_y = ball_speed_y * 2; 
    }
    else if(ball_speed_y > 0)
    {
      ball_speed_y = ball_speed_y / 2;
    }
    else
    {
      ball_speed_y = -1;
    }  
  }
  
---

Låg en kommentar i quit funktionen om att reseta player score, la in så den resetas där. tycker det är bra att det ligger i quit funktionen.

---

La till kommentarer lite här och var bland annat i collision filen.

---

TO DO
– Behöver en funktion eller dylikt för att ta oss tillbaka till menyn när en av spelarna vunnit så vi kan spela spelet igen.
– Byt ut reset funktionen som just nu ligger inlagd om det blir mål till score funktionen, denna måste testas för att se hur den funkar.

